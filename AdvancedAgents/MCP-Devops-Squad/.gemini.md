# MCP-DevOps-Squad

Production-ready Multi-Agent SRE System using Model Context Protocol (MCP) and Google Agent-to-Agent (A2A) patterns.

## Project Standards

- **Architecture:** Modular Python-based structure.
- **Communication:** Pydantic models for all inter-agent A2A hand-offs to ensure type safety.
- **MCP Integration:** All hardware/service access (Docker, GitHub, Filesystem) must go through MCP servers.
- **Security:**
    - No hardcoded credentials; use environment variables.
    - Janitor-Agent requires a "Human-in-the-loop" flag or "Validator" sign-off for destructive commands (`rm`, `kill`, etc.).
- **Logging:** Structured JSON logging in `/logs/` for audit trails using `structlog`.
- **Testing:** Every agent must have corresponding unit tests in `/tests/` and integration tests for the full A2A lifecycle.

## Directory Structure

- `src/agents/`: Individual class definitions for Monitor, Debugger, and Janitor agents.
- `src/mcp/`: Configuration and connection logic for MCP servers.
- `src/orchestrator/`: Lead-SRE logic handling A2A delegation loops.
- `config/`: YAML files for agent personas and threshold triggers.
- `logs/`: Structured JSON audit logs.
- `tests/`: Project test suite.
- `documentation/`: Project architecture, design decisions, and setup guides.

## Project Plan & Roadmap

### Phase 1: Foundation & Scaffolding (Completed)
- [x] Initial directory structure setup.
- [x] Define Pydantic models for structured inter-agent communication.
- [x] Create core agent stubs (`Lead-SRE`, `Monitor`, `Debugger`, `Janitor`).
- [x] Implement Mock MCP interactions for initial testing.
- [x] Integrate LangChain/VertexAI for `Lead-SRE` advanced reasoning.
- [x] Set up Human-In-The-Loop (HITL) and `Validator-Agent` logic.
- [x] Configure structured JSON logging (`structlog`).
- [x] Write initial Integration Tests.

### Phase 2: Live MCP Integration (Up Next)
- [ ] Connect `Monitor-Agent` to live Docker/Kubernetes MCP servers to stream actual metrics. (Priority: Monitor first)
- [ ] Connect `Debugger-Agent` to live GitHub and Filesystem MCP servers for real code/log analysis.
- [ ] Connect `Janitor-Agent` to a live Shell/CLI MCP server for executing approved fixes.

### Phase 3: Advanced Reasoning & Refinement
- [ ] Refine the LangChain Prompts for `Lead-SRE` to handle edge cases and ambiguous metrics.
- [ ] Add vector/memory storage for the Orchestrator to recall past incidents.
- [ ] Expand the `Validator-Agent` logic to handle more complex security rules and dynamic role-based access.
- [ ] Evolve the HITL system (keep using local JSON for now).

### Phase 4: CI/CD & Deployment
- [ ] Complete comprehensive Unit Test coverage for all agents.
- [ ] Create Dockerfiles to containerize the SRE Squad itself.
- [ ] Set up GitHub Actions for automated testing and linting (Native GitHub target).
